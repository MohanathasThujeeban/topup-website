# Application Configuration - Production (MongoDB Atlas)
spring.application.name=topup-backend
server.port=8080

# MongoDB Atlas Configuration
# MongoDB Atlas connection string configured (@ symbol URL-encoded as %40)
spring.mongodb.uri=${MONGODB_URI:mongodb+srv://thujee_db:Thujee%40tamil01923@topupdb.puesjra.mongodb.net/topup_db?retryWrites=true&w=majority&appName=topupdb}

# Auto-index creation
spring.data.mongodb.auto-index-creation=false

# JWT Configuration
app.jwt.secret=${JWT_SECRET:topupSecretKeyThatIsLongEnoughForHS512Algorithm123456789}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Email Configuration (Gmail example - update with your SMTP settings)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:thujeeforearn@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vblv pumf zyuw ecag}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application Specific Configuration
app.name=TopUp Pro
app.url=${APP_URL:https://your-production-domain.com}
app.support.email=${SUPPORT_EMAIL:support@topuppro.com}

# Verification Token Configuration
app.verification.email.expiration-hours=168  # 7 days
app.verification.password-reset.expiration-hours=24  # 24 hours

# Rate Limiting Configuration
app.rate-limit.email.max-attempts=5
app.rate-limit.email.time-window-minutes=60

# Logging Configuration
logging.level.com.example.topup.demo=INFO
logging.level.org.springframework.web=WARN
logging.level.org.springframework.security=WARN
logging.level.org.springframework.mail=WARN
logging.level.org.springframework.data.mongodb=INFO

# CORS Configuration
app.cors.allowed-origins=${CORS_ORIGINS:https://your-frontend-domain.com,http://localhost:3000}
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# Profile
spring.profiles.active=prod

# Actuator Configuration (for monitoring)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# SSL/HTTPS Configuration (uncomment for production)
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=password
# server.ssl.key-store-type=PKCS12